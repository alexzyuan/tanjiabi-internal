# 探嘉 BI AI 协作规则

本项目的 AI 实现规则以 `AGENTS.md` 为准。使用 Claude、Cursor 或其他只读取 `CLAUDE.md` 的工具时，也必须遵守同一套约束。

## 必读文档

实现前先阅读：

1. `AGENTS.md`：AI 工作方式、模块边界、重构检查点和前端验收要求。
2. `design.md`：唯一主设计规范，覆盖产品定位、信息架构、视觉规范、设计系统、运行架构和长期演进方向。

## 实现前结构设计

任何非简单文案或样式微调，都要先说明：

- 变更属于哪个页面、服务、适配器、mapper 或 UI 工具。
- 是否复用现有 helper、组件类、语义 token。
- 哪些文件需要改，哪些文件不应该改。
- 如何验证数据、交互和视觉结果。

不要默认把新逻辑塞进 `app.js` 或把一次性样式追加到 `styles.css` 尾部。必须先寻找现有模块、helper、组件类和 token。

## 阶段性收拢

完成一个小功能后，必须做一次局部整理：

- 删除重复逻辑。
- 合并临时补丁。
- 检查事件监听是否重复绑定。
- 检查表格和筛选更新是否有明显性能问题。
- 将稳定的样式抽成复用类或语义 token。

## 前端自查

涉及 UI 的任务，结束前要用浏览器实际检查。优先使用 Chrome DevTools MCP、Codex in-app browser 或 Playwright。

复杂组件应优先用 isolated component preview harness 临时单独渲染，集中检查布局、间距、动效、截图和 DOM 状态；交付前删除临时 harness，除非明确要保留为开发工具。
