# 探嘉 BI 领域词汇

本文件只记录业务词汇和边界，具体实现以代码与对应设计文档为准。

## 店铺身份

**店铺（Seller）**：领星中的一个可参与筛选、汇总和物流业务的销售店铺。`sid` 是稳定的外部身份，`name` 是领星店铺标识；国家和展示名是店铺属性，不是店铺身份本身。

**店铺目录（Seller Directory）**：当前可用的店铺身份集合。它必须来自领星运行时数据或其明确标注来源的缓存；静态配置不能凭空制造一个可用店铺。

**品牌标识**：运行时店铺名称中用于解析品牌或负责人规则的片段，例如 `tandanbo`、`tanjia`。品牌标识不是具体店铺，不能单独作为店铺选项或物流订单身份。

## 物流主体

**法定发件主体（Legal Sender Profile）**：由已确认的品牌归属规则解析出的公司名称、统一社会信用代码、联系人和地址。它决定报关和物流发件人信息，不能根据目的国家推断。

**店铺归属（Store Owner）**：领星运行时店铺名称包含 `tanjia`（或中文展示标识“探嘉”）时归属厦门探嘉，包含 `tandanbo`（或“坦蛋伯”）时归属厦门坦蛋伯，英文匹配忽略大小写且不受国家站点影响。未知品牌归属必须显式失败，不能默认归到任一公司。

## 销售事实

**订单利润日事实（Order Profit Daily Fact）**：一个自然日内，一个店铺 SID、一个 MSKU 和一种币种模式的可信销售利润度量。它不包含报表筛选条件或后来关联的 Listing 负责人。

**币种模式（Currency Mode）**：事实的金额口径，只能是人民币汇总 `CNY` 或保留上游本币的 `ORIGINAL`。不同币种模式的事实永不混用。

**实际币种（Actual Currency）**：事实实际携带的具体币种，例如 CNY、USD 或 EUR。它必须符合币种模式，同一事实身份不能静默出现多个实际币种。

**事实覆盖（Fact Coverage）**：某个自然日、店铺 SID 和币种模式已完整抓取的证据，包括上游真实返回零条事实的情况。它不是空缓存或“没有数据”的猜测。

**自定义费用月事实（Custom Fee Monthly Fact）**：一个自然月内，一个店铺 SID、费用类型和币种模式的费用分摊。它不人工拆分为订单利润日事实。

## Listing 负责人

**负责人有效期（Listing Owner Period）**：一个 `SID + MSKU` 在一段自然日范围内唯一有效的 Listing 负责人归属。检测到变更时从次日生效，不能重写此前历史。

**未分配负责人（Unassigned Listing Owner）**：领星明确返回负责人字段但内容为空时的有效负责人状态。它不等于历史未知。

**历史负责人未知（Historical Owner Unknown）**：可信负责人历史建立前的明确状态。不得使用当前负责人倒填这些日期。

**负责人修订号（Owner Revision）**：负责人有效期数据的单调递增版本，只在归属历史实际变化时增加。

## 派生结果

**销售事实修订号（Sales Facts Revision）**：已提交销售事实和覆盖证据的单调递增版本，只在完整原子刷新成功时增加。

**销售派生缓存（Derived Sales Cache）**：由销售事实和负责人有效期生成的可重算报表结果。它仅在事实修订号、负责人修订号、mapper 版本和 TTL 都匹配时有效。

## 词汇边界

- 需要表达具体店铺时，使用“店铺”或 `seller`，并携带 `sid` 或规范 `name`。
- 需要表达法定主体时，使用“法定发件主体”或 `legal sender`，不要用“目的国家”替代主体。
- `tanjia-eu-DE`、`tanjia-eu-UK` 等区域店铺进入运行时店铺目录后，即按 `tanjia` 品牌规则使用厦门探嘉主体；品牌规则不创建店铺，也不替代运行时目录校验。
- Listing 负责人是可变归属维度，不是 OrderProfit 事实主键或事实来源字段。
- “未分配负责人”表示上游明确空值；“历史负责人未知”表示缺少可信历史，二者不得互换。
