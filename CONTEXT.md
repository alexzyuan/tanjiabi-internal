# 探嘉 BI 领域词汇

本文件只记录业务词汇和边界，具体实现以代码与对应设计文档为准。

## 店铺身份

**店铺（Seller）**：领星中的一个可参与筛选、汇总和物流业务的销售店铺。`sid` 是稳定的外部身份，`name` 是领星店铺标识；国家和展示名是店铺属性，不是店铺身份本身。

**店铺目录（Seller Directory）**：当前可用的店铺身份集合。它必须来自领星运行时数据或其明确标注来源的缓存；静态配置不能凭空制造一个可用店铺。

**品牌标识**：运行时店铺名称中用于解析品牌或负责人规则的片段，例如 `tandanbo`、`tanjia`。品牌标识不是具体店铺，不能单独作为店铺选项或物流订单身份。

## 物流主体

**法定发件主体（Legal Sender Profile）**：由已确认的品牌归属规则解析出的公司名称、统一社会信用代码、联系人和地址。它决定报关和物流发件人信息，不能根据目的国家推断。

**店铺归属（Store Owner）**：领星运行时店铺名称包含 `tanjia`（或中文展示标识“探嘉”）时归属厦门探嘉，包含 `tandanbo`（或“坦蛋伯”）时归属厦门坦蛋伯，英文匹配忽略大小写且不受国家站点影响。未知品牌归属必须显式失败，不能默认归到任一公司。

## 词汇边界

- 需要表达具体店铺时，使用“店铺”或 `seller`，并携带 `sid` 或规范 `name`。
- 需要表达法定主体时，使用“法定发件主体”或 `legal sender`，不要用“目的国家”替代主体。
- `tanjia-eu-DE`、`tanjia-eu-UK` 等区域店铺进入运行时店铺目录后，即按 `tanjia` 品牌规则使用厦门探嘉主体；品牌规则不创建店铺，也不替代运行时目录校验。
