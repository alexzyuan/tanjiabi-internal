# 探嘉 BI 领域词汇

本文件只记录业务词汇和边界，具体实现以代码与对应设计文档为准。

## 店铺身份

**店铺（Seller）**：领星中的一个可参与筛选、汇总和物流业务的销售店铺。`sid` 是稳定的外部身份，`name` 是领星店铺标识；国家和展示名是店铺属性，不是店铺身份本身。

**店铺目录（Seller Directory）**：当前可用的店铺身份集合。它必须来自领星运行时数据或其明确标注来源的缓存；静态配置不能凭空制造一个可用店铺。

**店铺别名**：配置中用于匹配品牌或负责人规则的短名称，例如 `tandanbo`、`xiamentanjia`。别名不是具体店铺，不能单独作为店铺选项或物流订单身份。

## 物流主体

**法定发件主体（Legal Sender Profile）**：由已审核的店铺归属规则解析出的公司名称、统一社会信用代码、联系人和地址。它决定报关和物流发件人信息，不能根据目的国家推断。

**店铺归属（Store Owner）**：店铺品牌前缀对应的法定公司归属。未知店铺归属必须显式失败，不能默认归到任一公司。

## 词汇边界

- 需要表达具体店铺时，使用“店铺”或 `seller`，并携带 `sid` 或规范 `name`。
- 需要表达法定主体时，使用“法定发件主体”或 `legal sender`，不要用“目的国家”替代主体。
- `tanjia-eu-DE`、`tanjia-eu-UK` 等区域店铺名称只有在进入运行时店铺目录和对应主体审核后，才能用于 FBA 物流流程。
